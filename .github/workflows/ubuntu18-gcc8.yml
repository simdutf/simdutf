name: Ubuntu 18.04 CI (GCC 8)

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  ubuntu-build:
    if: >-
      ! contains(toJSON(github.event.commits.*.message), '[skip ci]') &&
      ! contains(toJSON(github.event.commits.*.message), '[skip github]')
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v3
      - name: Install GCC
        run: |
          sudo apt update
          sudo apt install gcc-8 g++-8
      - name: Use cmake
        run: |
          mkdir build &&
          cd build &&
          cmake ..  &&
          cmake --build .  --verbose &&
          ctest -j --output-on-failure
        env:
          CC: gcc-8
          CXX: g++-8
